# 98. ピクセル概念の現代/次世代化

## 18.1 視点

「ピクセルアート」は **視覚的な硬質さ** が特徴。ノノグラム (本作) は本来「数字パズル + 完成絵」のジャンルだが、完成絵をピクセルアートで表現するのに親和性が高い。本章では旧プラットフォーマー仕様の「視覚モード切替 (古典/現代/HD-2D/CRT)」を **本作向けに簡素化** し、クリア時の絵の演出 (色変化アニメ、§91) を中心に整理する。

## 18.2 二軸の設計

### 18.2.1 物理 (内部) は古典

- ノノグラム (本作) には物理は無い。旧仕様 §20 の SMB1 互換 (整数 + subpixel) は Round 5 で削除。
- 60Hz 固定。決定論。
- ノノグラム (本作) では「論理的に解ける気持ちよさ + 完成絵の達成感」を体験の主軸にする。

### 18.2.2 視覚 (外部) は段階選択

| モード | 表示の原則 |
|---|---|
| **クラシック** | nearest-neighbor、整数倍スケール、subpixel 位置を `>> 4` で px 整数化、シェーダ無し。古典 NES 風。 |
| **モダン** | nearest-neighbor を維持しつつ、subpixel 位置を **そのまま反映** (skipping the `>> 4` rounding)。120Hz/144Hz で滑らか。 |
| **HD-2D 風** | 高解像度バックグラウンド + Bloom + 加算光源 (コイン、ファイア)、ピクセル化されたキャラクタは保持。 |
| **CRT** | scanline + barrel distortion + 残光 (フェードフレーム平均)。WebGL/WebGPU 必須。 |

### 18.2.3 物理と視覚の独立性

- いずれの視覚モードでも物理は同一。同じ入力列で同じ frame の終了状態に到達する。
- 視覚モードはスコア/タイム/到達点に**絶対に影響しない**。

## 18.3 サブピクセルモーション (詳細)

- 古典 NES では "16 subpixel = 1 pixel" の整数演算で、表示時に `>> 4` で 1 px 単位で動く。これが「ぴょこぴょこした不連続」を生む。
- 本作の「モダン」モードは、表示時に subpixel をそのまま `(x_sub / 16) * scale` で実数座標にして描画 (ただし draw 時は `Math.round` を経由してピクセル境界に着く)。
- 究極的には WebGPU 使用時に **vertex で実数座標 + fragment で nearest sampling** にすれば「ぼやけずに滑らかに動くドット絵」が表現できる。

## 18.4 解像度の現代化

- 内部解像度は **16:9 で 480×270 px** を主軸とする (§17)。これは NES 解像度のおよそ 2 倍 (面積で 2 倍強)、ピクセルあたりの情報量を倍に。
- ノノグラムでは固定盤面 (5×5〜15×15) を全画面表示するため「視野」概念は不要。盤面内のセル + ヒント数字が読みやすい解像度を §70 で別途決定する。

## 18.5 ライト & シェーディング (HD-2D 風)

- **ボリューメトリックフォグ層**: 旧仕様の演出。ノノグラム (本作) では使用しない (盤面のみ表示するため奥行き演出は不要)。
- **ポイントライト**: コイン、ファイア弾、Star に半径付き加算光源 (Canvas2D の `globalCompositeOperation = 'lighter'` でも実現可能)。
- **ピクセル境界の保持**: シェーダはピクセル境界を超えて滲ませない (Bloom は別レイヤで合成)。

## 18.6 アニメーション現代化

- 古典: 各キャラ 2〜4 frame ループ。
- ノノグラム (本作) ではキャラクタアニメーション概念なし。クリア時に塗セルが色変化するアニメ (§91) のみ。
- 「踏みつぶし時の伸び」「ダメージ時の歪み」は **リアルタイム頂点歪み** で表現 (Canvas2D では transform、WebGPU では vertex shader)。

## 18.7 パーティクル

- 古典の "破片 4 個" を拡張。土埃、葉、火花を **ECS なしの軽量プール** で実装。ステージ環境ごとに別パレット。

## 18.8 次世代要素 (将来)

- **AI による背景生成**: ステージ JSON のテーマから背景レイヤをランタイム生成 (Stable Diffusion 系の事前生成済みアセットを動的合成)。
- **音声プロシージャル**: 環境音 (風、水) を Web Audio の合成で生成、ループ感を低減。
- **アクセシビリティ AI**: スクリーンリーダー向けにステージのテキスト要約を自動生成。
- これらは MVP 範囲外。

## 18.9 「ピクセルパーフェクト」と "解像度自由"

- ユーザー側で **ピクセル解像度を変える** (例: 1px = 1物理ピクセル / 1px = 4物理ピクセル) スライダーを提供。
- 解像度を変えても物理は不変、UI のヒット領域は別に確保。

## 18.10 採用と棄却の判断基準

| 機能 | MVP | v1.1 | v2.0 |
|---|---|---|---|
| クラシック表示 | ✅ | | |
| モダン表示 (subpixel motion) | ✅ | | |
| HD-2D 風 | (オプション) | ✅ | |
| CRT | (オプション) | ✅ | |
| AI 背景生成 | | | ✅ |
| プロシージャル音声 | | | ✅ |

- 本ドキュメントのスコープは **MVP** + **v1.1 の準備** まで。

## 18.11 ビルドチェーン (Round 2 / Issue #11)

「ピクセル概念の現代化」は描画・物理だけでなく開発者体験 (DX) にも適用する。**MVP のビルドチェーンは Vite 6+ で確定**する。

| 項目 | 採用 | 理由 |
| :--- | :--- | :--- |
| バンドラ / dev サーバ | **Vite 6 系** | エコシステムの成熟 (`vite-plugin-pwa` / `vite-plugin-react` / Pixi.js v8 連携) と HMR の安定性。Bun の bundler は WebGPU 周りで一部 plugin が未対応 (2026-05 時点) のため MVP では Vite を主、Bun はランタイム / PM / テストランナーに限定 (§14.9 / §14.14) |
| トランスパイラ | esbuild (内蔵) | TS → JS の高速変換 |
| 本番ビルド | Rollup (Vite 6 内蔵) | tree-shake と code-split が成熟 |
| Service Worker | `vite-plugin-pwa` | Workbox ラッパー、precache / SWR 戦略を JSON で宣言 |

> 上表の正確なバージョン番号 (Vite / `vite-plugin-pwa` 等) は §14.14 / Issue #10 (T5) PR の確定スタック表で固定する。

> 互換性: Pixi.js v8 (WebGPU/WebGL2/Canvas2D の透過切替, §11.2) と Vite 6 の相性は確認済み (Pixi 公式テンプレート / 各種スターターで採用実績)。

## 18.12 PWA 範囲の確定 (Round 2 / Issue #11)

PWA 機能は **オフラインキャッシュ + フルスクリーン起動の 2 機能のみ** に絞る (旧 Round 1 までの「マルチプレイヤー連携用に Push API を将来導入」想定は破棄)。本方針は Round 5 ノノグラム転換後も継続。

| 機能 | 採否 | 理由 |
| :--- | :--- | :--- |
| Service Worker (precache + SWR) | **採用** | 通信圏外でも MVP 内ステージはプレイ可能。バージョン管理とキャッシュ無効化は §94.10 の手順に従う |
| Web App Manifest (`display: standalone` / `display_override: ['fullscreen','standalone']`) | **採用** | ホーム画面追加 → フルスクリーン起動。アドレスバーが消えるため、§17.5 の `dvh`/`svh` 設計と組み合わせて没入感を最大化 |
| インストール可能 (Add to Home Screen) | **採用** | iOS / Android / Desktop Chrome すべてで対応 |
| Push API | **不採用** | ユーザーへのプッシュ通知はゲーム体験を阻害するノイズ。将来の機能拡張 (UGC 共有等) でも別メカニズム (Cloudflare Workers + REST) を選ぶ |
| Background Sync | **不採用** | サーバ依存のオンライン専用ゲーム前提でないため不要。スコア送信が必要になった時点で再評価 |
| Background Fetch | **不採用** | 大容量ステージ DLC の事前配信が必要になるまで保留 |
| Web Share Target | **不採用** | ユーザー生成リプレイの共有機能を入れる段階で再評価 (MVP 外) |
| Periodic Background Sync | **不採用** | ユーザーが起動していないときのサーバ通信は本作の SLO に合わない |

> オフラインキャッシュとフルスクリーン起動の 2 機能だけで、本作の「ピクセル概念の現代化 = ネイティブアプリと並ぶ没入感を Web で実現」は十分達成できる。

## 18.13 古典 → 現代化の対応関係

参考までに、本作で「古典の概念」をどう「現代の手段」で再実装したかを §98 で集約しておく:

| 古典 (旧プラットフォーマー仕様 Round 1-4) | ピクセルズ (Round 5+) |
| :--- | :--- |
| 6502 NES の固定 60Hz CPU ループ | rAF + 固定タイムステップ 60Hz (§14.3) |
| ゼロページの整数演算 (8bit) | TypedArray (Int32Array) SoA (§20.1.1) |
| OOP / アクター連結リスト | bitECS (§14.2 / §14.2.1) |
| 8bit 機の固定パレット | スプライトアトラス + WebGPU 16bit float カラー (§11.2.1 / §18.5) |
| カートリッジ ROM | Service Worker precache (§14.10 / §18.12) |
| ハンド組み立てステージデータ | Valibot Schema-first JSON (§8.2.2 / §94.2.3) |
| キーボードマトリクス スキャン | Gamepad API (ポーリング) + イベント駆動キーボード (§9.6.2) |
| CRT 表示 | WebGPU/WebGL2 シェーダによる CRT モード (§11.10) |
