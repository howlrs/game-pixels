# 99. 未決事項とレビュー履歴

本書執筆時点で結論に至っていない論点と、Gemini レビューの採否一覧をここに集約する。

## 19.1 未決事項

### 19.1.1 内部解像度のデフォルト

- 候補 A: 256×240 (NES 互換、4:3)
- 候補 B: 480×270 (16:9 拡張)
- 現状: B 採用。ただし「クラシック準拠 を強く打ち出す路線」では A を採用する判断もあり得る。デザイン方針が決まったら再検討。

### 19.1.2 World Map の有無

- 候補 A: SMB1 風 「クリアで自動で次へ」
- 候補 B: SMB3 風 ステージ選択マップ
- 現状: B (再挑戦・ショートカットの自由度が高いため)。Web の "ステージ単発挑戦" UX とも親和的。

### 19.1.3 リスポーン時のサイズ

- 候補 A: Small に強制 (古典互換)
- 候補 B: 死亡前のサイズで復帰 (モダン)
- 現状: A。ただし a11y "ジャンプアシスト" 等を有効にしている場合は B も提供可。

### 19.1.4 ステージ進行の自動セーブ

- 各ステージのチェックポイントを永続化するか?
- 現状: 永続化しない (古典互換)。a11y モードでは別途検討。

### 19.1.5 マルチプレイ拡張

- ローカル協力 (画面分割なし、画面共有) は技術的に検討余地あり。
- ネットワーク同期は本書範囲外。決定論を活かしたロックステップ方式が候補。

### 19.1.6 Mario Maker 風 UGC

- ステージエディタの提供。コミュニティ機能の有無。
- 範囲外。アーキテクチャ (§94) はデータ駆動なので拡張余地あり。

### 19.1.7 数値定数の最終調整

- 本書では SMB1 由来の数値を起点としているが、Web の入力レイテンシ (タッチ等) を考慮し、**全プレイテストののち調整**。
- 調整は `core/physics-constants.ts` で集中管理 (§94)。

### 19.1.8 ECS への移行

- MVP は OOP。アクター数や拡張性で限界が見えたタイミングで `bitECS` 等への移行を検討。
- 移行コストとリスクは別途見積もり。

### 19.1.9 マネタイズ

- 範囲外。広告/IAP/購入のいずれも MVP に含めない。
- 設計上はクラウド同期の存在を前提に「アカウント識別」を抽象化しておく。

## 19.2 Gemini レビュー反映ログ

### Round 1 (2026-05-05, gemini-3.1-pro-preview)

#### A 群 (§20/§30/§40, GitHub Issue #1)

| 指摘 | 採否 | 反映先 |
|---|---|---|
| ジャンプ中断ロジック矛盾 (§2.2.4 vs §4.2.1) | 採用 | §4.2.1 を二重重力に一本化、`vy` 直接クリップを削除 |
| 片道床タイマーの危険性 (§3.7) | 採用 | "AABB 重なり消失まで Set で無視" 状態管理に変更 |
| 数値表記不整合 (§2.2.3) | 採用 | `-0x40 (-64) subpixel/frame` に統一 |
| 坂スナップ時の天井 headroom (§3.6) | 採用 | スナップ前に上方 1 px の空間判定を追加 |
| 自動段差乗り越え暴発 (§3.5) | 採用 | 5 条件 (接地/水平移動/前足元 Solid/上空 1 px Empty/頭上 Empty) を厳密化 |
| L+R 同時入力の SOCD (§4.7) | 採用 | 既定「後押し優先」、古典挙動はオプション化 (§2.2.5) |
| 32 bit 整数化の徹底 (§2.1, §94) | 採用 | `(x | 0)` 強制 + TypedArray の SoA 検討を §94 で参照 |
| 衝突 while ループの max_iterations (§3.4) | 採用 | `MAX_RESOLVE_ITERATIONS = 8` を擬似コードに反映 |

#### B 群 (§50/§70/§80/§93, GitHub Issue #2)

| 指摘 | 採否 | 反映先 |
|---|---|---|
| ファイアボール `>>` シフトの罠 (§50) | 採用 | `Math.trunc(vy * 7 / 8)` に変更 |
| カメラ垂直追従のスナップ (§70) | 採用 | 着地時も補間係数 0.25 を当てる |
| Uint16Array Base64 のエンディアン (§80) | 採用 | DataView でリトルエンディアン固定 |
| 敵 HP 統一 (§50) | 採用 | `hp: number` を全敵に持たせ、踏み = 1 dmg に統一 |
| 赤ノコノコ崖判定ルックアヘッド (§50) | 採用 | 進行方向に 4 subpixel 先で判定 |
| カメラシェイク後のクランプ (§70) | 採用 | シェイク加算後に再クランプ |
| §93 セクション番号整合 | 採用 | 章タイトルとサブセクション番号の対応を §00 で明記 |
| セーブ HMAC (§93) | 部分採用 | クラウド同期実装時のみ。MVP 範囲外と明記 |
| バックアップ退避 (§93) | 採用 | `save_backup_v<ver>` キーに退避してから既定セーブで起動 |

#### C 群 (§90/§91/§95/§97/§98, GitHub Issue #3)

| 指摘 | 採否 | 反映先 |
|---|---|---|
| `touch-action: none` の限界 (§90) | 採用 | `preventDefault({ passive: false })` を併用 |
| リプレイ NaN 汚染対策 (§90) | 採用 | InputSnapshot バリデータを §94 で要求 |
| 補間描画の 1 frame 遅延 (§91) | 採用 | 既定は補間、低レイテンシモードで外挿選択肢 |
| シマーリング対策 (§91 / §98) | 採用 | カメラ → エンティティ → 描画の座標スナップ順序を明文化 |
| `\| 0` の Deopt 懸念 (§95) | 採用 | TypedArray + SoA 推奨、`\| 0` は局所最適化 |
| 100vh → dvh/svh (§97) | 採用 | `100dvh` 採用、`visualViewport` は古環境フォールバック |
| フローティングパッド MVP 化 (§97) | 採用 | 設定でフローティング切替を MVP に含む |

## 19.3 設計レビューの観点 (チェックリスト)

実装着手前に以下を再確認する:

- [ ] 物理定数 (§20) は単位・上限・下限が明示されているか
- [ ] 衝突 (§30) は軸分離・サブステップ・ゴーストバーテックス対策が記述されているか
- [ ] プレイヤー (§40) のサイズ階層と状態機械が一致しているか
- [ ] 敵 (§50) の踏みつけ可否、甲羅、連鎖スコアが網羅されているか
- [ ] アイテム (§60) の抽選とブロック種別が網羅されているか
- [ ] カメラ (§70) は両方向 + 縦持ちで破綻しない設計か
- [ ] ワールド (§80) のデータ形式は拡張可能か (Pipe Warp, Trigger)
- [ ] 入力 (§90) は 3 系統 + リマップ + ゲームパッドのリビルドが具体か
- [ ] 描画 (§91) は Canvas2D / WebGL / WebGPU の縮退が安全か
- [ ] オーディオ (§92) の自動再生制限と SE プールの扱いが明確か
- [ ] セーブ (§93) はマイグレーションとフォールバックを持つか
- [ ] アーキテクチャ (§94) の依存方向は決定論を保つか
- [ ] パフォーマンス (§95) のフレームバジェットがモバイルで実現可能か
- [ ] アクセシビリティ (§96) は OFF/ON が同等の動作か
- [ ] クロスデバイス (§97) はセーフエリアと方向回転を扱うか
- [ ] ピクセル現代化 (§98) は物理と視覚を独立に扱うか
